<div class="cool" >
  <h1>All Articles</h1>
  <p>Browse through the library:</p>
  <ol>
  <% @articles.each do |article|%>
    <li> Title: <%=link_to article.title, article_path(article)%>
    <br>
    <%= link_to 'Delete here', article_path(article),
      method: :delete,
      data: { confirm: "Are you sure?" } %>
    <p>____________</p>
    </li>
  <% end %>
  </ol>

  <button type="button" class="button"><%=link_to 'Add Article', articles_new_path%></button>
</div>
